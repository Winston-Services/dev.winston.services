import React from 'react';

import { Container, Typography } from '@mui/material';

function index() {
  return (
    <Container>
      <Typography variant="h3">Current holdings</Typography>
    </Container>
  );
}

export default index;
