import React from 'react';

import TextField from './TextField';

const AddressLine2TextField = (props) => {
  return <TextField label="Address Line 2" name="address2" size="small" {...props} />;
};

export default AddressLine2TextField;
